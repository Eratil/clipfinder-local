# Supported Windows/Python 3.11 builds use the tested transitive versions in
# this constraints file. Keep it in source control so a release cannot silently
# pick a different native runtime than the one validated by CI.
-c constraints-win-py311.txt

fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
pywebview==6.2.1
pydantic-settings==2.10.1
faster-whisper==1.1.1
ctranslate2==4.8.1
huggingface-hub==0.36.2
sentence-transformers==5.0.0
# Sentence embeddings do not need CUDA: transcription uses CTranslate2 and
# the separate CUDA/cuDNN add-on. A CPU Torch wheel keeps the base installer
# portable and avoids bundling DLLs that require a local CUDA runtime.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu
scenedetect[opencv-headless]==0.6.6
numpy==2.1.3
yt-dlp==2026.7.4
certifi==2026.7.22
truststore==0.10.4
scipy==1.14.1
